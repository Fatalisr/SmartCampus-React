import PropTypes from "prop-types";

const ListeSalles = (props) =>{
    return(
        <>
        </>
    )
}

ListeSalles.propTypes = {
    exampleProp: PropTypes.string.isRequired,
}
export default ListeSalles